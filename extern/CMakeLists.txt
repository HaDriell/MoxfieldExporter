# This file is here because some extern projects are not using CMake 
# I can't version the CMakeLists.txt file inside the git submodule tree

# JSON
add_subdirectory(JSON)

# CPR (curl wrapper for C++)
add_subdirectory(cpr)